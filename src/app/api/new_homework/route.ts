export async function POST(req: Request) {
    try {
        const {
            hubId,
            title,
            content,
            createdByUserId } = await req.json();


    } catch (error) {

    }
}